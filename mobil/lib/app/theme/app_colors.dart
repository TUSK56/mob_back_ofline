import 'package:flutter/material.dart';

class AppColors {
  // Dark Theme Palette
  static const darkBg = Color(0xFF0F172A); // Deeper navy
  static const darkSurface = Color(0xFF1E293B); // Slate surface
  static const darkPrimary = Color(0xFF3B82F6); // Bright blue
  static const darkAccent = Color(0xFFF59E0B); // Amber accent
  static const darkText = Color(0xFFF8FAFC); // Almost white
  static const darkMuted = Color(0xFF94A3B8); // Muted slate

  // Light Theme Palette
  static const lightBg = Color(0xFFF9F5F1);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightPrimary = Color(0xFF4A6ED1);
  static const lightAccent = Color(0xFFFF7A2A);
  static const lightText = Color(0xFF0B1020);
  static const lightMuted = Color(0xFFB59A90);

  // Status Colors
  static const success = Color(0xFF10B981);
  static const error = Color(0xFFEF4444);
  static const warning = Color(0xFFF59E0B);
  static const info = Color(0xFF3B82F6);
}
